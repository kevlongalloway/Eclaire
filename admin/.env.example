# Base URL of the Éclaire API worker (no trailing slash). Read at build time.
# The admin key is NOT stored here — it's entered at the login screen and must
# match ADMIN_API_KEY configured on the API worker in Cloudflare.
VITE_API_BASE=https://your-worker.workers.dev
