# Base URL of the Éclaire API worker (no trailing slash). Read at build time.
# Login credentials are NOT stored here — you sign in with the username and
# password configured as ADMIN_USERNAME / ADMIN_PASSWORD secrets on the API worker.
VITE_API_BASE=https://your-worker.workers.dev
